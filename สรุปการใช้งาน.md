# 📋 สรุปการใช้งาน

## ✅ สิ่งที่ทำได้แล้ว

1. ✅ **ดึงข้อมูลหวยได้แล้ว** - โปรแกรมรันสำเร็จ
2. ✅ **เก็บข้อมูลลง database** - ไฟล์ `lottery.db`

---

## 📊 ข้อมูลเก็บอยู่ที่ไหน?

**ไฟล์:** `C:\POF\lotto\lottery.db`

- เก็บข้อมูลทุกครั้งที่รันโปรแกรม
- ไม่หายไปจนกว่าจะลบไฟล์
- อัพเดทข้อมูลที่มีอยู่แล้ว และเพิ่มข้อมูลใหม่

---

## ⏰ ตั้งเวลาให้รันอัตโนมัติ

### วิธีง่ายที่สุด:

**Double click `ตั้งเวลารันอัตโนมัติ.bat`**

จะสร้าง Task ที่รันทุก 6 ชั่วโมงอัตโนมัติ

### ตัวอย่างการตั้งเวลา:

```bash
# รันตามวันออกรางวัลจริง (แนะนำ) ⭐
# จันทร์, พุธ, ศุกร์ เวลา 20:30 น.
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc weekly /d MON,WED,FRI /st 20:30

# รันทุกวันเวลา 20:30
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc daily /st 20:30

# รันทุก 6 ชั่วโมง
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc hourly /mo 6
```

**หมายเหตุ:** ต้องรัน Command Prompt แบบ Administrator

---

## 🔍 ดูข้อมูล

**Double click `ดูข้อมูล.bat`**

จะแสดง:
- จำนวนข้อมูลทั้งหมด
- หวยพัฒนาล่าสุด 10 รายการ
- หวยลาสีล่าสุด 10 รายการ

---

## 📁 ไฟล์ที่สำคัญ

- `รันโปรแกรม.bat` - รันโปรแกรม (แสดงผล)
- `รันโปรแกรม_เงียบ.bat` - รันโปรแกรม (เงียบๆ สำหรับ Task Scheduler)
- `ตั้งเวลารันอัตโนมัติ.bat` - สร้าง Scheduled Task
- `ดูข้อมูล.bat` - ดูข้อมูลที่ scrap มา
- `lottery.db` - ไฟล์ database ที่เก็บข้อมูล

---

## 🚀 ขั้นตอนต่อไป

1. **ตั้งเวลา:** Double click `ตั้งเวลารันอัตโนมัติ.bat`
2. **ตรวจสอบ:** ดู Task ใน Task Scheduler
3. **ดูข้อมูล:** Double click `ดูข้อมูล.bat` เมื่อต้องการดูข้อมูล

---

## ⚠️ หมายเหตุ

- **คอมพิวเตอร์ต้องเปิดอยู่** - Task จะรันได้เมื่อคอมเปิดอยู่
- **ต้องมี Internet** - ต้องเชื่อมต่ออินเทอร์เน็ตเพื่อดึงข้อมูล
- **ข้อมูลไม่หาย** - เก็บใน `lottery.db` จนกว่าจะลบไฟล์
