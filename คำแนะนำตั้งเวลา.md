# ⏰ คำแนะนำการตั้งเวลาให้รันอัตโนมัติ

## 📊 ข้อมูลเก็บอยู่ที่ไหน?

**ไฟล์:** `lottery.db` ในโฟลเดอร์โปรเจกต์ (`C:\POF\lotto\lottery.db`)

- ข้อมูลจะถูกเก็บอัตโนมัติทุกครั้งที่รันโปรแกรม
- ไม่หายไปจนกว่าจะลบไฟล์
- ถ้ารันซ้ำ จะอัพเดทข้อมูลที่มีอยู่แล้ว และเพิ่มข้อมูลใหม่

---

## ⏰ วิธีตั้งเวลาให้รันอัตโนมัติ

### วิธีที่ 1: ใช้ Windows Task Scheduler (แนะนำ)

#### ขั้นตอนที่ 1: สร้าง Task

**Double click `ตั้งเวลารันอัตโนมัติ.bat`**

หรือใช้ Command Prompt (ต้องรันแบบ Administrator):

```bash
# รันทุก 6 ชั่วโมง
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม.bat" /sc hourly /mo 6

# รันทุกวันเวลา 20:00
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม.bat" /sc daily /st 20:00

# รันทุก 30 นาที
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม.bat" /sc minute /mo 30
```

#### ขั้นตอนที่ 2: ตรวจสอบ Task

```bash
schtasks /query /tn "LotteryScraper"
```

#### ขั้นตอนที่ 3: ลบ Task (ถ้าต้องการ)

```bash
schtasks /delete /tn "LotteryScraper" /f
```

---

### วิธีที่ 2: ใช้ GUI (Windows Task Scheduler)

1. เปิด **Task Scheduler**:
   - กด `Windows + R`
   - พิมพ์ `taskschd.msc` แล้วกด Enter

2. สร้าง Task ใหม่:
   - คลิก **"Create Basic Task"** ทางขวา
   - ตั้งชื่อ: `LotteryScraper`
   - เลือกความถี่: **Daily** หรือ **Hourly**
   - ตั้งเวลา: เช่น 20:00 หรือทุก 6 ชั่วโมง
   - เลือก **"Start a program"**
   - Browse หาไฟล์: `C:\POF\lotto\รันโปรแกรม.bat`
   - คลิก **Finish**

---

## 📅 ตัวอย่างการตั้งเวลา

### รันตามวันออกรางวัลจริง (แนะนำ) ⭐
**วันออกรางวัล: จันทร์, พุธ, ศุกร์ เวลา 20:30 น.**
```bash
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc weekly /d MON,WED,FRI /st 20:30
```

### รันทุกวันเวลา 20:30
```bash
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc daily /st 20:30
```

### รันทุก 6 ชั่วโมง
```bash
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc hourly /mo 6
```

### รันทุก 30 นาที
```bash
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม.bat" /sc minute /mo 30
```

### รันทุกวันจันทร์-ศุกร์ เวลา 20:30
```bash
schtasks /create /tn "LotteryScraper" /tr "C:\POF\lotto\รันโปรแกรม_เงียบ.bat" /sc weekly /d MON,TUE,WED,THU,FRI /st 20:30
```

---

## 🔍 ตรวจสอบ Task

### ดูรายการ Task ทั้งหมด
```bash
schtasks /query
```

### ดูรายละเอียด Task
```bash
schtasks /query /tn "LotteryScraper" /v /fo list
```

### รัน Task ทันที (ทดสอบ)
```bash
schtasks /run /tn "LotteryScraper"
```

---

## 📁 ข้อมูลที่เก็บ

### ตาราง: `lottery_results`

**ฟิลด์ที่เก็บ:**
- `source_id` - ID จาก API
- `round_id` - ID รอบหวย
- `round_date` - วันที่ออกผล
- `round_number` - หมายเลขรอบ
- `win_number` - เลขที่ออก ⭐
- `lot_number` - หมายเลข lot
- `year_id` - ปี
- `lottery_type` - ประเภทหวย (phathana/lasi)
- `is_close_sale` - ปิดการขายแล้วหรือยัง
- `round_status` - สถานะรอบ
- `is_jackpot` - เป็นแจ็คพอตหรือไม่
- `created_at` - วันที่สร้าง
- `updated_at` - วันที่อัพเดท

---

## 🔄 การอัพเดทข้อมูล

- **ถ้ามีข้อมูลอยู่แล้ว:** จะอัพเดทข้อมูลเดิม
- **ถ้าไม่มีข้อมูล:** จะเพิ่มข้อมูลใหม่
- **ไม่ซ้ำ:** ใช้ `source_id` เป็น unique key

---

## 📊 ดูข้อมูล

**Double click `ดูข้อมูล.bat`** หรือใช้ SQLite Browser

---

## ⚠️ หมายเหตุ

1. **คอมพิวเตอร์ต้องเปิดอยู่** - Task Scheduler จะรันได้เมื่อคอมเปิดอยู่
2. **ต้องมี Internet** - ต้องเชื่อมต่ออินเทอร์เน็ตเพื่อดึงข้อมูล
3. **ไฟล์ Database** - ข้อมูลเก็บใน `lottery.db` ในโฟลเดอร์โปรเจกต์

---

## 🚀 สรุป

1. **ข้อมูลเก็บที่ไหน?** → `lottery.db` ในโฟลเดอร์โปรเจกต์
2. **ตั้งเวลายังไง?** → Double click `ตั้งเวลารันอัตโนมัติ.bat` หรือใช้ Task Scheduler
3. **ดูข้อมูลยังไง?** → Double click `ดูข้อมูล.bat`
