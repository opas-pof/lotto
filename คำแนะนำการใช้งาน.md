# 📖 คำแนะนำการใช้งานสำหรับผู้เริ่มต้น

## 🎯 ไฟล์ที่สร้างมาแล้วคืออะไร?

### ไฟล์หลักที่ต้องรู้:

1. **`main.py`** ⭐
   - ไฟล์หลักที่ต้องรัน
   - เมื่อรันแล้วจะ scrap ข้อมูลหวยและเก็บลง database

2. **`scraper.py`**
   - โค้ดสำหรับดึงข้อมูลจากเว็บ laodl.com
   - **ไม่ต้องเปิดเอง** - ถูกเรียกใช้โดย main.py

3. **`database.py`**
   - โค้ดสำหรับจัดการ database
   - **ไม่ต้องเปิดเอง** - ถูกเรียกใช้โดย main.py

4. **`requirements.txt`**
   - รายชื่อ library ที่ต้องติดตั้ง
   - **ไม่ต้องเปิดเอง** - ใช้ตอนติดตั้ง

5. **`README.md`**
   - คู่มือโปรเจกต์
   - **อ่านได้** - มีข้อมูลทั่วไป

---

## 🚀 วิธีใช้งาน (แบบง่ายๆ)

### ขั้นตอนที่ 1: ติดตั้ง Python ⚠️ สำคัญมาก!

**ถ้าเห็น error "pip is not recognized" = ยังไม่ได้ติดตั้ง Python**

1. ไปที่ https://www.python.org/downloads/
2. ดาวน์โหลด Python 3.11 หรือสูงกว่า
3. ติดตั้ง **อย่าลืมติ๊ก "Add Python to PATH"** ✅
   - ถ้าไม่ติ๊ก โปรแกรมจะรันไม่ได้!

**ดูรายละเอียดเพิ่มเติม:** เปิดไฟล์ `วิธีติดตั้งPython.md`

### ขั้นตอนที่ 2: เปิด Command Prompt

1. กด `Windows + R`
2. พิมพ์ `cmd` แล้วกด Enter
3. ไปที่โฟลเดอร์โปรเจกต์:
   ```
   cd C:\POF\lotto
   ```

### ขั้นตอนที่ 3: ติดตั้ง Library ที่ต้องใช้

พิมพ์คำสั่งนี้:
```
pip install -r requirements.txt
```

รอให้เสร็จ (อาจใช้เวลา 1-2 นาที)

### ขั้นตอนที่ 4: รันโปรแกรม

พิมพ์คำสั่งนี้:
```
python main.py
```

### ขั้นตอนที่ 5: ดูผลลัพธ์

โปรแกรมจะแสดง:
- กำลังดึงข้อมูลหวย...
- พบข้อมูลหวยพัฒนา X รายการ
- พบข้อมูลหวยลาสี X รายการ
- ข้อมูลผลหวยล่าสุด

---

## 📁 ไฟล์ที่สร้างขึ้นอัตโนมัติ

เมื่อรันโปรแกรมแล้ว จะมีไฟล์ใหม่:

- **`lottery.db`** - ไฟล์ database ที่เก็บข้อมูลหวย
  - เปิดดูได้ด้วยโปรแกรม SQLite Browser
  - หรือใช้ Python script อื่นๆ

---

## 🎨 วิธีรันแบบง่าย (Double Click)

ถ้าไม่อยากพิมพ์ command line:

1. ดูไฟล์ `รันโปรแกรม.bat` (ฉันจะสร้างให้)
2. Double click ที่ไฟล์นั้น
3. โปรแกรมจะรันอัตโนมัติ

---

## ❓ คำถามที่พบบ่อย

**Q: ต้องเปิดไฟล์อะไรบ้าง?**
- A: **ไม่ต้องเปิดไฟล์เอง** แค่รัน `main.py` ก็พอ

**Q: ไฟล์ไหนสำคัญที่สุด?**
- A: `main.py` - ไฟล์นี้คือตัวรันโปรแกรม

**Q: ต้องแก้ไขไฟล์อะไรไหม?**
- A: **ไม่ต้อง** โค้ดพร้อมใช้งานแล้ว

**Q: รันแล้วได้ไฟล์อะไร?**
- A: ได้ไฟล์ `lottery.db` ที่เก็บข้อมูลหวย

**Q: ดูข้อมูลใน database ยังไง?**
- A: ใช้โปรแกรม SQLite Browser หรือเขียน Python script ใหม่

---

## 🔧 ถ้าเกิดปัญหา

### ปัญหา: "python is not recognized"
- **แก้ไข**: ติดตั้ง Python และติ๊ก "Add Python to PATH"

### ปัญหา: "pip is not recognized"
- **แก้ไข**: ติดตั้ง Python ใหม่ และติ๊ก "Add Python to PATH"

### ปัญหา: "Module not found"
- **แก้ไข**: รัน `pip install -r requirements.txt` อีกครั้ง

---

## 📝 สรุปสั้นๆ

1. ✅ ติดตั้ง Python
2. ✅ เปิด Command Prompt
3. ✅ ไปที่โฟลเดอร์ `C:\POF\lotto`
4. ✅ รัน `pip install -r requirements.txt`
5. ✅ รัน `python main.py`
6. ✅ ดูผลลัพธ์

**แค่นี้เอง!** 🎉
