# 📊 วิธีดูผลลัพธ์ที่ Scrap มา

## 🎯 ผลลัพธ์อยู่ที่ไหน?

### 1. **ใน Terminal/Command Prompt** (ขณะรันโปรแกรม)

เมื่อรัน `รันโปรแกรม.bat` หรือ `py main.py` จะเห็นผลลัพธ์ทันที:

```
==================================================
ระบบ Scrap ข้อมูลผลหวยจาก laodl.com
==================================================

กำลังดึงข้อมูลหวย...

พบข้อมูลหวยพัฒนา 100 รายการ
บันทึกข้อมูล phathana จำนวน 100 รายการ

พบข้อมูลหวยลาสี 50 รายการ
บันทึกข้อมูล lasi จำนวน 50 รายการ

==================================================
ข้อมูลผลหวยล่าสุด:
==================================================

หวยพัฒนา:
  วันที่: 2026-01-19
  รอบที่: 8
  เลขที่ออก: 907789

หวยลาสี:
  วันที่: 2025-07-31
  รอบที่: 153
  เลขที่ออก: 500964
```

### 2. **ในไฟล์ Database** (`lottery.db`)

ข้อมูลทั้งหมดถูกเก็บในไฟล์ `lottery.db` ในโฟลเดอร์โปรเจกต์

**ตำแหน่ง:** `C:\POF\lotto\lottery.db`

---

## 🔍 วิธีดูข้อมูล

### วิธีที่ 1: ใช้ Script ที่สร้างให้ (แนะนำ) ⭐

**Double click ที่ `ดูข้อมูล.bat`**

จะแสดง:
- จำนวนข้อมูลทั้งหมด
- หวยพัฒนาล่าสุด 10 รายการ
- หวยลาสีล่าสุด 10 รายการ
- ข้อมูลล่าสุดของทั้งสองประเภท

### วิธีที่ 2: ใช้ Command Prompt

```bash
py view_data.py
```

### วิธีที่ 3: ใช้ SQLite Browser

1. ดาวน์โหลด **DB Browser for SQLite** จาก: https://sqlitebrowser.org/
2. เปิดไฟล์ `lottery.db`
3. ดูข้อมูลในตาราง `lottery_results`

### วิธีที่ 4: ใช้ Python Script เอง

สร้างไฟล์ Python ใหม่:

```python
from database import DatabaseManager

db = DatabaseManager()
results = db.session.query(LotteryResult).all()
for r in results:
    print(f"{r.round_date} - {r.win_number}")
db.close()
```

---

## 📁 ไฟล์ที่เกี่ยวข้อง

- **`lottery.db`** - ไฟล์ database ที่เก็บข้อมูลทั้งหมด
- **`view_data.py`** - Script สำหรับดูข้อมูล
- **`ดูข้อมูล.bat`** - Double click เพื่อดูข้อมูล

---

## ❓ คำถามที่พบบ่อย

**Q: ข้อมูลถูกเก็บที่ไหน?**
- A: ในไฟล์ `lottery.db` ในโฟลเดอร์โปรเจกต์

**Q: ดูข้อมูลยังไง?**
- A: Double click `ดูข้อมูล.bat` หรือใช้ SQLite Browser

**Q: ข้อมูลจะหายไหม?**
- A: ไม่หาย ข้อมูลอยู่ในไฟล์ `lottery.db` จนกว่าจะลบไฟล์

**Q: Scrap ข้อมูลใหม่จะทับข้อมูลเก่าไหม?**
- A: ไม่ทับ จะอัพเดทข้อมูลที่มีอยู่แล้ว และเพิ่มข้อมูลใหม่

---

## 🚀 ขั้นตอนการใช้งาน

1. **Scrap ข้อมูล:**
   - Double click `รันโปรแกรม.bat`
   - รอให้เสร็จ (10-30 วินาที)

2. **ดูผลลัพธ์:**
   - ดูใน Terminal ขณะรัน
   - หรือ Double click `ดูข้อมูล.bat` หลังจากรันเสร็จ

3. **ดูข้อมูลใน Database:**
   - ใช้ SQLite Browser เปิดไฟล์ `lottery.db`

---

## 📝 สรุป

- **ผลลัพธ์ทันที:** ดูใน Terminal ขณะรัน
- **ข้อมูลทั้งหมด:** ดูด้วย `ดูข้อมูล.bat` หรือ SQLite Browser
- **ไฟล์ Database:** `lottery.db` ในโฟลเดอร์โปรเจกต์
